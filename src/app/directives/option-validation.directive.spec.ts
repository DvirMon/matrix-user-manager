import { OptionValidationDirective } from './option-validation.directive';

describe('OptionValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new OptionValidationDirective();
    expect(directive).toBeTruthy();
  });
});
